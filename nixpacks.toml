[phases.setup]
nixPkgs = ["nodejs_24", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @workspace/api-server --filter @workspace/docuforge run build"]

[start]
cmd = "pnpm --filter @workspace/api-server run start"
